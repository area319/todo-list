export type loginPropType = {
    email: string,
    password: string,
}

export type signupPropType = {
    email: string,
    password: string,
}

export type resetPasswordPropTypes = {
    email: string,
    curPassword: string,
    newPassword: string,
}
